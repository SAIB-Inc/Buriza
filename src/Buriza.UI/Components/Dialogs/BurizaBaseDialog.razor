@using Buriza.UI.Resources
@namespace Buriza.UI.Components.Dialogs

<MudDialog
    Class= "@DialogClass"
    TitleClass="@DialogTitleClass"
    ContentClass="@DialogContentClass"
    ActionsClass="@DialogActionsClass"
>
    <TitleContent>
        <div class="flex items-center justify-between">
            <MudText Class="!text-xl !font-semibold !tracking-[0.1px] !text-[var(--mud-palette-text-primary)] flex-1 text-center min-[425px]:text-left">
                @Title
            </MudText>
            <MudIconButton
                Icon="@BurizaIcons.CloseSymbolIcon"
                Class="!hidden !p-0 [&_.mud-icon-root]:!text-[var(--mud-palette-text-secondary)] min-[425px]:!block"
                OnClick="OnClose"
            />
        </div>
    </TitleContent>
    <DialogContent>
        @DialogContent
    </DialogContent>
    <DialogActions>
        @DialogActions
    </DialogActions>
</MudDialog>
